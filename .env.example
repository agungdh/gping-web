# Base URL of the gping HTTP server (the one started with `--http`).
# Same-origin by default (the UI and API are served from one port). Set this
# only when the UI is hosted separately from the gping API, e.g. during `vite dev`.
# PUBLIC_GPING_API=http://127.0.0.1:8080
